# DPC-AIO 1.0.0 Release Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate new modules/UI into the monolithic APK, bump 1.0.0, preserve all 0.9 tests, and create a clean source ZIP with independent verification statuses.

**Architecture:** Add offline modules to settings/app dependencies, register UI/recovery components, extend host/pre-push suites, update diagnostics/release docs, and package a secret-free source tree.

**Tech Stack:** Gradle Kotlin DSL, Kotlin host tests, Python contract tests, release shell gates, ZIP/SHA-256.

**Spec:** `docs/superpowers/specs/2026-08-22-dpc-aio-1.0.0-offline-application-control-design.md`

## Global Constraints
- Version: `1.0.0`, versionCode `18`.
- Existing 0.9 enrollment tests remain green.
- APK_BUILD_VERIFIED is not claimed unless Gradle actually compiles.
- Live offline/device-owner/work-profile statuses are not inferred from source tests.

---

### Task 1: Module and app wiring
- [ ] Add `:offline-core` and `:offline-android` to `settings.gradle.kts` and app dependencies.
- [ ] Register Offline Setup/recovery components in manifest/dashboard.
- [ ] Add 1.0 capability/diagnostic fields.
- [ ] Run project/module/dashboard contracts.

### Task 2: Release gates
- [ ] Create `tools/tests/test_100_release_gate_contract.py` requiring offline, permission-manager and component-manager tests in host/pre-push scripts.
- [ ] Verify RED.
- [ ] Wire all new Kotlin mains/Python contracts into `tools/run_host_tests.sh` and `tools/release/verify-before-push.sh`.
- [ ] Verify GREEN.

### Task 3: Version/docs/plugin
- [ ] Bump app to versionCode 18/versionName 1.0.0.
- [ ] Update README, release notes, validation JSON and package info with separated verification statuses.
- [ ] Bump companion plugin contract metadata to 0.1.7 and verify deterministic packaging.

### Task 4: Final verification/package
- [ ] Run all Kotlin host mains.
- [ ] Run all Python/Node contract suites and pre-push gates.
- [ ] Attempt `./gradlew :app-dpc:assembleEnterpriseDebug`; record exact blocker if environment prevents dependency resolution.
- [ ] Create clean staging without build/cache/private-key artifacts.
- [ ] Re-run secret/non-SDK/release gates on staging.
- [ ] Package password `123`, verify archive integrity after fresh extraction, and write SHA-256.
