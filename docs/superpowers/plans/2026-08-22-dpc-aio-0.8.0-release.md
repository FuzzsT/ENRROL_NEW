# DPC-AIO 0.8.0 Release Integration Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate all Enterprise Operations Center surfaces, bump 0.8.0/16 and package a verified password-protected release source ZIP.

**Architecture:** Reuse existing host tests, release scanners, dual-QR verifier and companion plugin checks. New contract tests are wired into the same pre-push gate.

**Tech Stack:** Gradle/Android, Kotlin/JVM tests, Python contract tests, shell release gates, zip.

**Spec:** `docs/superpowers/specs/2026-08-22-dpc-aio-0.8.0-enterprise-operations-center-design.md`

## Global Constraints
- `versionName=0.8.0`, `versionCode=16`.
- Private lab key excluded from release staging.
- Password for user release ZIP: `123`.
- Android assemble may only be claimed PASS on exit code 0.

---

### Task 1: Dashboard, diagnostics and version
**Files:** Modify `AioDashboardActivity.kt`, `DpcDiagnosticsSnapshot.kt`, `apps/dpc/app/build.gradle.kts`, release docs and tests.
- [ ] RED release/version/diagnostics contract tests for 0.8.0 fields.
- [ ] Add operation-center entries and diagnostics summaries; bump version.
- [ ] Verify GREEN.

### Task 2: Wire gates
**Files:** Modify `tools/run_host_tests.sh`, `tools/release/verify-before-push.sh`; new test files from all prior plans.
- [ ] RED test ensuring every new contract is invoked by pre-push.
- [ ] Wire tests and scanners.
- [ ] Verify all constituent gates GREEN.

### Task 3: Final package verification
- [ ] Run policy-core Kotlin mains and all Python contract tests.
- [ ] Run `verify-before-push.sh` and provisioning-service tests.
- [ ] Attempt `./gradlew :app-dpc:assembleEnterpriseDebug`; record actual result.
- [ ] Stage release excluding transient files and private lab key.
- [ ] Run secret/non-SDK scans on staging.
- [ ] Create `DPC-AIO-0.8.0-ENTERPRISE-OPERATIONS-123.zip` with password `123` and preserve executable bits.
- [ ] Test archive integrity and repeat critical tests after fresh extraction.
- [ ] Write SHA-256 and validation JSON with explicit PASS/BLOCKED statuses.
